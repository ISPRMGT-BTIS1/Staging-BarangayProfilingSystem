import { AppRouter } from './Router'

/**
 * Root application component.
 * Providers are composed in main.tsx — this component only renders the router.
 */
export default function App() {
  return <AppRouter />
}
